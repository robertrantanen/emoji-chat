<!DOCTYPE html>
<html>
  <head>
    <title>Chat</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body style="background-color:yellow;">
    <div class="ps-5 pt-3 pb-3" style="background-color:greenyellow;">
      <%= link_to 'Chat rooms', rooms_path %>
      <%= link_to 'Create new chat room', new_room_path %>
      <% if current_user %>
        <%= current_user.username %> signed in
        <%= link_to "Log out", signout_path, data: { "turbo-method": :delete } %>
      <% else %>
        <%= link_to 'Sign up', new_user_path %>
        <%= link_to 'Log in', new_session_path %>
      <% end %>
    </div>
    <div class="ps-5">
    <h1>Emoji chat <%= image_tag ("emoji/smiley.png"), class: "img-responsive"%> <%= image_tag ("emoji/speech_balloon.png"), class: "img-responsive"%> </h1>
    <%= yield %>
    </div>
    <script src="/assets/jquery.js" type="text/javascript"></script>
    <%= javascript_include_tag 'rails_emoji_picker' %>
  </body>
</html>
